// DualSocketDemo -- legacy compatibility shim
//
// The canonical Dell-host example now lives in `HostNumaProbe.chpl`.
// This file remains only so existing local commands and references do not break
// while the Dell repo stops advertising the same canonical demo name as
// XoxdWM.

include "HostNumaProbe.chpl";
