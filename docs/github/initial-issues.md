# Initial Issues

This file contains ready-to-create issue titles and concise bodies for the first GitHub setup pass.

## Issue 1

Title:

`[meta] establish GitHub milestones, labels, and issue templates`

Body:

Set up the repository operating model for this hardware project.

Deliverables:

- milestones M0-M4
- label taxonomy
- issue templates and PR template pushed to default branch

Acceptance criteria:

- repo has a usable issue workflow before the first serious measurement cycle lands

## Issue 2

Title:

`[measure] capture lower rail and rear interface geometry`

Body:

Measure the OEM lower rail and rear interface well enough to unlock the first fit coupons.

Target feature IDs:

- `IF-003`
- `IF-004`
- `IF-005`
- `IF-006`
- `IF-007`
- `IF-013`
- `IF-014`

Acceptance criteria:

- all target fields captured with scale photos
- no guessed lower-rail dimensions
- sufficient evidence to model lower-rail and rear-corner coupons

## Issue 3

Title:

`[measure] capture top latch geometry and motion`

Body:

Measure the OEM top retention and record latch motion clearly enough to decide whether latch reuse is viable.

Target feature IDs:

- `IF-008`
- `IF-009`
- `IF-010`
- `IF-011`
- `IF-012`

Acceptance criteria:

- latch location and engagement features are measured from chassis datums
- motion is documented with photos or sketch
- enough confidence exists to build a latch coupon without guessing

## Issue 4

Title:

`[measure] capture GPU protrusion and connector bend envelope`

Body:

Measure the actual installed GPU protrusion and the cable bend geometry that drives shell height and cable opening placement.

Target feature IDs:

- `GPU-002`
- `GPU-006`
- `GPU-007`

Acceptance criteria:

- measured with the GPU and cables in a realistic installed state
- enough data exists to stop treating shell height as a guess

## Issue 5

Title:

`[measure] capture real cable bundle envelope and preferred exit zone`

Body:

Measure the actual cable bundle and terminated connector cluster that must pass through the new enclosure.

Target feature IDs:

- `CBL-001`
- `CBL-002`
- `CBL-003`
- `CBL-004`
- `CBL-005`

Acceptance criteria:

- real compressed bundle dimensions are captured
- preferred opening zones are marked and photographed
- enough data exists to compare brush-slot and split-entry approaches
