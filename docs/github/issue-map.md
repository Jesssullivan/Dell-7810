# Issue Map

Live issue map for the current first-wave tracker.

## M0 Project Framing

- `#1` establish GitHub milestones, labels, and issue templates
- `#3` define issue taxonomy and prototype evidence rules

## M1 Interface Capture

- `#4` capture lower rail and rear interface geometry
- `#5` capture top latch geometry and motion
- `#2` capture GPU protrusion and connector bend envelope
- `#6` capture real cable bundle envelope and preferred exit zone
- `#7` model lower rail fit coupon
- `#8` model latch fit coupon
- `#9` model rear-corner fit coupon
- `#10` model cable opening test plate
- `#11` print and evaluate first interface coupon set

## Practical execution order

1. `#3` lock evidence rules and traceability.
2. `#4`, `#5`, `#2`, `#6` gather measurement evidence.
3. `#7`, `#8`, `#9`, `#10` produce the first coupon set.
4. `#11` validates the first coupon set physically.

## Notes

- Issue numbers are creation-order numbers, not workflow order.
- The lower-rail/rear-interface issue is `#4`, not `#3`.
