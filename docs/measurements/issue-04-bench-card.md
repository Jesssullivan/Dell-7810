# Issue 04 Bench Card

Target:

- `#4` lower rail and rear interface geometry

Unlocks:

- `#7` lower rail fit coupon
- `#9` rear-corner fit coupon

## Bring

- calipers
- steel rule
- square
- masking tape
- marker
- phone
- card stock

## Capture order

1. Context photos
   full lower rail, rear top corner, rear edge overlap/hook
2. Broad lower-rail placement
   `IF-003`, `IF-006`
3. Local lower-rail profile
   `IF-004`, `IF-005`, `IF-007`
4. Rear edge and corner
   `IF-013`, `IF-014`
5. Stamped offsets near interface
   `IF-016`
6. Hook-in motion sanity check
   `PATH-001`

## Minimum photo proof

- one full-length lower-rail photo with scale
- one datum photo for bottom reference
- one lower-rail close-up with scale
- one rear-corner photo with scale
- one card-transfer photo if geometry is awkward

## Rules

- measure each critical dimension three times
- do not average away profile drift
- if calipers cannot reach cleanly, transfer profile to card
- do not close `#4` unless `#7` and `#9` can start without guessed geometry

## Record here

- `data/measurements/issue-04-lower-rail-rear.csv`
- then summarize in issue `#4`
